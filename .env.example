# CEMUhook Motion Server Configuration
# Copy this file to .env and update with your settings

# SSL/HTTPS Configuration
# Set USE_SSL to true to enable HTTPS with Let's Encrypt
USE_SSL=false

# Required when USE_SSL=true
# Your email for Let's Encrypt notifications
SSL_EMAIL=

# Your domain name or public IP address
SSL_DOMAIN=

# Port Configuration
# HTTP port (use 80 for Let's Encrypt, requires sudo/root)
HTTP_PORT=8080

# HTTPS port (use 443 for standard HTTPS, requires sudo/root)
HTTPS_PORT=8443

# Certificate storage directory
# CERT_DIR=./certificates

# Example configurations:
#
# Local development (HTTP only):
# USE_SSL=false
# HTTP_PORT=8080
#
# Production with domain (HTTPS):
# USE_SSL=true
# SSL_EMAIL=admin@example.com
# SSL_DOMAIN=motion.example.com
# HTTP_PORT=80
# HTTPS_PORT=443
#
# Production with public IP (HTTPS):
# USE_SSL=true
# SSL_EMAIL=admin@example.com
# SSL_DOMAIN=123.45.67.89
# HTTP_PORT=80
# HTTPS_PORT=443
